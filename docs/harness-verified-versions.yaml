# fedi-test-harness 互換性検証済み upstream バージョン台帳
#
# モロヘイヤ (mulukhiya-toot-proxy) が chubo2 の fedi-test-harness を使って
# 「バニラ Mastodon / Misskey との互換性を実機検証した最新バージョン」と
# 「upstream リリースを最後にチェックした日」を記録する。
#
# 用途（セッション組み込み方式。専用の cloud/cron ジョブは使わない）:
#   - docs/CLAUDE.md の「セッション開始時の同期手順」で、last_checked から一定
#     日数（既定: 4 日）以上経過していたら upstream 最新リリースを確認し、
#     verified より新しい stable / RC があれば検証を促す。確認したら last_checked
#     を当日に更新する。
#   - chubo2 側 fedi-test-harness/{mastodon,misskey}/.env.example の
#     MASTODON_VERSION / MISSKEY_VERSION と対になる値。互換性検証を終えて
#     harness の既定を bump したら、verified もこのファイルで同じ版に更新する。
#
# upstream リリースの参照先（GitHub）:
#   - Mastodon : mastodon/mastodon      （stable は vX.Y.Z、RC は vX.Y.Z-rc.N）
#   - Misskey  : misskey-dev/misskey    （stable は YYYY.M.P、alpha/beta は prerelease）
#     ※ Docker Hub の misskey/misskey とは別。GitHub では misskey-dev/misskey。
#
# 検証フロー（新バージョン検知時）:
#   1. cd ~/repos/chubo2/fedi-test-harness/<mastodon|misskey>
#   2. ./scripts/update-version.sh   # upstream 最新に追従
#   3. ./scripts/reset.sh            # 新イメージ + クリーン DB で再構築
#   4. mulukhiya 側で account / status / controller 等の harness テストを実走
#      (要 mulukhiya 自前 Redis localhost:6379)
#   5. 緑なら chubo2 .env.example と本ファイルの verified を新バージョンに更新
#
# 運用方針の詳細は MEMORY の feedback_upstream-release-harness-verification 参照。

mastodon:
  verified: v4.6.4
  verified_at: '2026-07-30'

misskey:
  verified: 2026.6.0
  verified_at: '2026-06-23'

# upstream 最新リリースを最後に確認した日（同期手順が更新する）。
last_checked: '2026-07-30'

# 2026-07-30 チェック結果:
#   - Mastodon: stable v4.6.4（2026-07-27）を fedi-test-harness で検証・verified 昇格。
#     07-28 に「キュアスタ！カットオーバー後に実施」として保留していた分の消化。
#     v4.6.4 イメージ + クリーン DB で再構築（update-version.sh → reset.sh）し、mulukhiya
#     フルスイートを実走して **879 tests / 1767 assertions / 0 failures / 0 errors /
#     11 omissions（100% passed）**＝退行ゼロ。omission 11 件はすべて既知の harness 側
#     ギャップ（chubo2#63 デーモン層・HTTP/webhook エンドポイント・streaming_api・nodeinfo の
#     https/http 不整合、chubo2#64 メディア/ハッシュタグ seed 未整備）で Mastodon 版とは無関係。
#     DB 直読み経路（status / account / postgres）は omission ゼロで全緑。失敗が 1 件も無い
#     ため v4.6.3 との差分実走（07-08 のような比較）は不要と判断した。
#     chubo2 .env.example も v4.6.4 へ更新済み（pooza/chubo2#96）。
#     参考: 07-08 の baseline は 831 tests / 6 failures / 41 errors だったが、その後の
#     #4447 triage・PR#4450 で環境起因の失敗が omission に整理され 0 failures / 0 errors に
#     なった（MEMORY の project_harness-zero-error-goal の目標を達成）。
#   - Misskey: 最新 stable は 2026.6.0（2026-06-22）のままで verified と同一。**2026.7.0-rc.0 が
#     07-28 に出た**が prerelease。Misskey は stable リリース後数日で検証する方針なので対応不要。

# 2026-07-28 チェック結果:
#   - Mastodon: stable v4.6.4（2026-07-27）が出た。セキュリティリリース（権限判定の不備
#     GHSA-7jvv-fhmg-wpfw / GHSA-hx34-2pfw-2qfj、IPv4-compatible IPv6 経由の SSRF 保護
#     バイパス GHSA-vwhj-3g83-v276）で、4.5.14 / 4.4.21 も同時リリース。assets 再コンパイル必須。
#     **本番 3 台には 2026-07-28 に適用済み**（pooza/mastodon の bshockdon → delmulin / curesta
#     へ 07-27 にマージ済み）。セキュリティ修正のため harness 検証より先に本番適用した。
#     harness 検証（+ chubo2 .env.example と本ファイルの bump）は**キュアスタ！カットオーバー
#     (lbock → gomander, 07-28) 後に実施**する。変更ログ上モロヘイヤに効きうるのは Web Push
#     subscription 削除の CSRF 期待値変更 (#39918) のみだが、モロヘイヤの sw_subscription 経路は
#     Misskey 専用なので影響なしと判断。
#   - Misskey: 最新 stable は 2026.6.0（2026-06-22）のままで verified と同一。2026.7.0 は
#     beta.6 まで進んだが prerelease。対応不要（stable 化したらリリース後数日で検証）。
#
# 2026-07-20 チェック結果:
#   - Mastodon: 最新 stable は v4.6.3（2026-07-03）のままで verified と同一。RC もなし。対応不要。
#   - Misskey: 最新 stable は 2026.6.0 のまま。2026.7.0 は alpha.6 まで出ているが prerelease。対応不要。
#
# 2026-07-13 チェック結果:
#   - Mastodon: 最新 stable は v4.6.3 のままで verified と同一。RC もなし。対応不要。
#   - Misskey: 最新 stable は 2026.6.0 のままで verified と同一。対応不要。
#
# 2026-07-08 チェック結果:
#   - Mastodon: stable v4.6.3（2026-07-03）を fedi-test-harness で検証・verified 昇格。
#     v4.6.3 と v4.6.0 で mulukhiya フルスイートを clean 再構築のうえ実走・比較し
#     結果が完全一致（831 tests / 6 failures / 41 errors、失敗集合も 45 件同一）。
#     残存失敗は streaming/listener・メディア/タグ seed 無し・media_catalog 既定OFF
#     起因の既存ギャップで Mastodon 版とは無関係、contract テストは全緑＝退行ゼロ。
#     chubo2 .env.example も v4.6.3 へ更新済み（pooza/chubo2#61）。
#   - Misskey: verified 2026.6.0 が依然最新 stable（2026.6.1 は alpha 止まり）。対応不要。
