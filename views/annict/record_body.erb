<%# encoding: UTF-8 -%>
<% if params.dig(:record, :record, :comment).present? -%>

<%= params.dig(:record, :record, :comment).ellipsize(config['/annict/chars']) %>
<% end -%>
<%= params.dig(:record, :work, :title).to_hashtag %>
https://annict.com/works/<%= params.dig(:record, :work, :id) %>/episodes/<%= params.dig(:record, :episode, :id) %>
