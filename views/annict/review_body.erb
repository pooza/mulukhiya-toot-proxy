<%# encoding: UTF-8 -%>
<% if params.dig(:review, :body).present? -%>

<%= params.dig(:review, :body).ellipsize(config['/annict/chars']) %>
<% end -%>
<%= Mulukhiya::AnnictService.create_review_uri(params.dig(:review, :work, :id)) %>
<%= params.dig(:review, :work, :title).to_hashtag %>
