<%# encoding: UTF-8 -%>
<% if params.dig(:review, :body).present? -%>

<%= params.dig(:review, :body).ellipsize(200) %>
<% end -%>
https://annict.com/works/<%= params.dig(:review, :work, :id) %>/records
