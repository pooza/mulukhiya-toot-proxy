<%= params.dig(:record, :episode, :work, :title) %>
<% if params.dig(:record, :episode, :numberText).present? || params.dig(:record, :episode, :title).present? -%>
<% if params.dig(:record, :episode, :numberText).present? %><%= params.dig(:record, :episode, :numberText) %><% end -%>
<% if params.dig(:record, :episode, :title).present? %>「<%= params.dig(:record, :episode, :title) %>」<% end -%>
<% end -%>
を視聴。
