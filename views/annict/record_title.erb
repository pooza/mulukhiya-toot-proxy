<%# encoding: UTF-8 -%>
<%= params.dig(:record, :work, :title) %>
<% if params.dig(:record, :episode, :number_text).present? || params.dig(:record, :episode, :title).present? -%>
<% if params.dig(:record, :episode, :number_text).present? %><%= params.dig(:record, :episode, :number_text) %><% end -%>
<% if params.dig(:record, :episode, :title).present? %>「<%= params.dig(:record, :episode, :title) %>」<% end -%>
<% end -%>
を視聴。
