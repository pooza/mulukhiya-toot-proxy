<%# encoding: UTF-8 -%>
SELECT
  keyword AS tag
FROM
  (
    SELECT
      jsonb_array_elements(keywords)->>0 AS keyword
    FROM
      (SELECT jsonb_array_elements(keywords) AS keywords FROM antenna) AS t
  ) AS t
WHERE keyword LIKE '#%'
GROUP BY
  keyword
