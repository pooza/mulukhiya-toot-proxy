{
  viewer {
    annictId
    name
    username
    avatarUrl
    works {
      nodes {
        annictId
        title
        seasonYear
        officialSiteUrl
        viewerStatusState
      }
    }
  }
  searchWorks (
    titles: <%= params[:title].to_json %>,
    orderBy: {field: SEASON, direction: DESC}
  ) {
    edges {
      node {
        annictId
        title
        seasonYear
        officialSiteUrl
      }
    }
  }
}
