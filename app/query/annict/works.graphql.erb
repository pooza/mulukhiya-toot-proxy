{
  viewer {
    annictId
    name
    username
    avatarUrl
  }
  searchWorks (
    titles: "<%= params[:title] %>",
    orderBy: {field: SEASON, direction: DESC}
  ) {
    edges {
      node {
        annictId
        title
        seasonYear
        officialSiteUrl
      }
    }
  }
}
