{
  searchWorks (annictIds: [<%= params[:id].to_json %>]) {
    nodes {
      annictId
      title
      seasonYear
      episodes (orderBy: {field: SORT_NUMBER, direction: DESC}) {
        nodes {
          annictId
          numberText
          title
        }
      }
    }
  }
}
