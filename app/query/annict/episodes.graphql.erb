{
  searchWorks (annictIds: [<%= params[:id].to_i.to_json %>]) {
    nodes {
      annictId
      title
      seasonYear
      episodes (orderBy: {field: SORT_NUMBER, direction: DESC}) {
        nodes {
          annictId
          numberText
          title
        }
      }
    }
  }
}
