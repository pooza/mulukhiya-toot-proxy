ja:
  dry_validation:
    errors:
      array?: 配列ではありません。
      bool?: 真偽値ではありません。
      gt?: 値が小さすぎます。
      hash?: ハッシュではありません。
      int?: 数値ではありません。
      key?: 空欄です。
      lt?: 値が大きすぎます。
      max_size?: 要素数が多すぎます。
      min_size?: 要素数が少なすぎます。
      str?: 文字列ではありません。
