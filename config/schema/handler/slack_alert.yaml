label: Slackアラート
description: エラー発生時にSlack Webhookへアラートを送信します
properties:
  disabled:
    type: boolean
  hooks:
    items:
      format: uri
      type: string
    type: array
  timeout:
    minimum: 1
    type: integer
required:
  - hooks
type: object
