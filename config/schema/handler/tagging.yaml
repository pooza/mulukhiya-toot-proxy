label: タグ処理
description: 投稿末尾のハッシュタグを正規化・整理します
properties:
  disabled:
    type: boolean
  normalize:
    properties:
      rules:
        items:
          properties:
            normalized:
              type: string
            source:
              type: string
          required:
            - source
            - normalized
          type: object
        type: array
    required:
      - rules
    type: object
  timeout:
    minimum: 1
    type: integer
type: object
