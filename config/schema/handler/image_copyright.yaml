label: 画像著作権表示
description: 画像付き投稿に著作権表示メッセージを追加します
properties:
  disabled:
    type: boolean
  message:
    type: string
  tag:
    type: string
  timeout:
    minimum: 1
    type: integer
  url:
    format: uri
    type: string
required:
  - tag
  - url
type: object
